# 直接依赖声明（Worker 独立环境，禁止混入主后端 requirements.txt）。
# UZI Stage 1/2 所需的 AkShare / Pandas / Playwright 只允许出现在本文件。
#
# 锁定与隔离要求（文档 §19.2）：
#   cd backend/uzi_worker
#   python3 -m venv .venv
#   ./.venv/bin/python -m pip install -r requirements.lock
#   PLAYWRIGHT_BROWSERS_PATH="$PWD/.playwright" \
#     ./.venv/bin/python -m playwright install chromium
#
# 注意：禁止在宿主机执行 `playwright install --with-deps`。

# Worker 自身 API 依赖
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
pydantic>=2.7.0

# UZI Stage 1/2 运行依赖（对齐上游 requirements.txt）
akshare>=1.14.0
yfinance>=0.2.40
baostock>=0.9.1
pandas>=2.0.0
requests>=2.31.0
ddgs>=9.0.0
mplfinance>=0.12.10b0
playwright>=1.42.0
rich>=13.0.0

# 测试
pytest>=8.0.0
